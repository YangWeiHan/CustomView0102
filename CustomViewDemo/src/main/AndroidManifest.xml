<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mryang.customview0102">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.CustomViewActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.CircleActivity" />
        <activity android:name=".activity.RectangleActivity" />
        <activity android:name=".activity.TriangleActivity" />
        <activity android:name=".activity.SectorActivity" />
        <activity android:name=".activity.EllipseActivity" />
        <activity android:name=".activity.CurveActivity" />
        <activity android:name=".activity.CylindricalGraphActivity" />
        <activity android:name=".activity.ShowActivity" />
        <activity android:name=".lottery.LotteryActivity" />
        <activity android:name=".indicator.activity.IndicatorActivity"></activity>
    </application>

</manifest>